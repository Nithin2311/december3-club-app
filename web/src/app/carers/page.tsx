import Placeholder from "@/components/Placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Carer Accounts"
      blurb="Link up to 5 trusted carers — opt-in support, never silent surveillance."
      todo={["Invite / manage up to 5 carers", "User-controlled visibility settings", "API: /carers"]}
    />
  );
}
