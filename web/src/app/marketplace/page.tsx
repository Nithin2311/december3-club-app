import Placeholder from "@/components/Placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Marketplace"
      blurb="A disability-first, members-only marketplace to sell art, crafts, and services from an accessible profile."
      todo={["Product catalog + accessible listing form", "Members-only storefront", "API: /marketplace"]}
    />
  );
}
