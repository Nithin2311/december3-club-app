import Placeholder from "@/components/Placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Events"
      blurb="Inclusive calendar that flags Sensory-Friendly, Hybrid, and accessible events and matches them to your needs."
      todo={["Event list + filters (Sensory-Friendly / Hybrid / Wheelchair)", "Match events to a user accessibility profile", "API: /events"]}
    />
  );
}
